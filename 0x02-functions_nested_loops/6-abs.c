#include "holberton.h"
/**
 * _abs - check the code for Holberton School students.
 * @n: is an integer argument
 * Return: 0 for succes.
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
